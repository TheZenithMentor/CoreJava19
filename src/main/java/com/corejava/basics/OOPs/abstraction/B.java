package com.corejava.basics.OOPs.abstraction;

abstract public class B {

    public void demo()
    {
        System.out.println("demo");
    }

    abstract  public void test();
}

/*
*  can we create object for abstract class
*  can we create referenve variable for the abstract class
*  can we write static member inside the abstract class
*  can we write non static member inside the abstract class
*  can i write main method inside the abstact class.
*  can we have atleast one abstract and one concrete method inside the concrete class
*  cam we have 2 abstract method inside the abstract class
* can we write 2 concrete method inside the abstract class
*
*
* */